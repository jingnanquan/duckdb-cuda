当前问题：
1.目前test_bitmap_join_perf.cpp中测试中，GetForceResolvedPK和SetForceResolvedPK的实现仅为force_bhj设计，未尝试正常查询下能否通过LoadFromJson(string(kDataDir) + "/bitmap_join_meta.json"); 获取其中的主外键信息以进入bitmap路径。
2.PlanComparisonJoin代码中，默认hash_join.bitmap_join_resolved.build_is_pk_side = true; 但是整体设计中，这里应该能自动判断当前plan中该节点的build表是否为PK和probe表是否为FK，进而来设置use_bitmap_join是true或false
3.当前的bitmapjoin实现中，build侧的joinkeys使用的仍是pk、probe侧的joinkeys使用的仍是fk。能否扩展让其使用_rowid列和ref列。新增场景：尝试带fliter和projection的sql，并且执行时这些算子能感知到_rowid和ref列的存在，从而对其自动保留
4.多次join中，后续hashjoin的build侧已经不是纯粹的单表了。此时如何保证即使最终sql输出不包含_rowid和ref列，plan所有operator都能感知并保留_rowid列和ref列（有可能出现rowid同名的情况），使后面的hashjoin也能使用bitmapjoin进行加速，又如何在最终输出sink之前projection删掉_rowid列和ref列。新增场景：尝试多hashjoin的复杂sql，上层的hashjoin是否也能自动启用bitmapjoin
5.最终测试要求，能直接运行tpch5、tpch9、tpch10等sql，并输出详细的总时间对比、各个operator的pref对比
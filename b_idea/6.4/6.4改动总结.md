代码部分：


测试部分：
 新增两个 settings（open_perfect_join / open_bitmap_join）
src/include/duckdb/main/settings.hpp：新增 OpenBitmapJoinSetting（idx=95, 默认 false）和 OpenPerfectJoinSetting（idx=96, 默认 true），并把 MaxSettingIndex 从 95 → 97。
src/main/config.cpp：在 internal_options 表中按字母序插入两个 setting，并把 alias 表中受影响的 option_index 同步 +2（profiling_output 116→118、worker_threads 130→132、user 131→133）。
2. physical_hash_join.cpp 中接入开关
HashJoinGlobalSinkState 构造函数：use_bitmap_join = op.use_bitmap_join && Settings::Get<OpenBitmapJoinSetting>(context_p)；perfect_join_executor 仅在 Settings::Get<OpenPerfectJoinSetting>(context_p) 为 true 时才创建。
Finalize：增加 if (sink.perfect_join_executor) 守卫，避免开关关闭时空指针访问。
3. 让 BHJ 真的能跑起来
bitmap_join_meta.hpp / .cpp：新增 SetForceResolvedPK / GetForceResolvedPK（测试期可注入 PK binding，绕过 module 6.6 的 column-binding 反查）。
plan_comparison_join.cpp：当 force_bitmap_join + BHJ-eligible 时，从注册表取出 PK binding 填入 bitmap_join_resolved，原地启用 use_bitmap_join。


当前问题：
1.GetForceResolvedPK和SetForceResolvedPK的实现仅为force_bhj设计，未尝试正常查询下能否通过LoadFromJson(string(kDataDir) + "/bitmap_join_meta.json");获取其中的主外键信息。
2.PlanComparisonJoin代码中，默认hash_join.bitmap_join_resolved.build_is_pk_side = true; 但是整体设计中，这里应该能自动根据build表是否为PK和probe表是否为FK来设置use_bitmap_join是true或false
3.上述1，2关系到我们如何扩展test_bitmap_join_perf.cpp中的测试，首先就是1.尝试join路径中带fliter和projection 2.尝试多次join 3.能否直接运行tpch的一些复杂查询
4.多次join中，后续hashjoin的build侧已经不是纯粹的单表了。此时如何保证所有operator都能感知并保留_rowid列和ref列，使后面的hashjoin也能使用这些列进行加速，又如何在最终输出sink之前projection删掉_rowid列和ref列


细节相关问题：
1.ToUnifiedFormat函数作用是什么？似乎只针对duckdb自身封装的vector
2.如果bitmap规定的这么严格，那么确保稠密就需要，中间全都是连续无重复的。所以在新增隐藏列时是否严格按照了这个规则？
3.ValidityMask的setvalidunsafe是什么意思？
4.bitmap用ValidityMask可行吗？每一位是否只用1bit表示？
答：validity_mask 是一个 uint64_t 数组，每个元素（entry）管理 64 行的有效性。所以完全可行

第一阶段：低风险优化
只改 bitmap_hash_join_executor.hpp/.cpp 和 physical_hash_join.cpp 极少量代码：

BitmapJoinLocalState 复用 valid_rows / rowids。
local bitmap 懒初始化，去掉 local state 构造期全尺寸初始化。
VARCHAR payload 改为按列锁。
将 ToUnifiedFormat 尽量移出 VARCHAR 锁。
这阶段不会改变数据结构，风险低。

第二阶段：新增 compact 模式
只在 BitmapJoinExecutor 内部加模式，不动优化器：

新增 BitmapJoinPayloadMode。
构造期根据 B、估计 N、payload 宽度选择 dense/compact。
dense 路径保持现状。
compact 路径新增：
compact build buffer；
sparse global bitmap write；
touched words；
word_base rank mapping；
compact payload materialization；
compact probe selection。
第三阶段：可选 fallback hash mapping
如果实测 word_base + popcount 在 probe 热点明显，再加可选：